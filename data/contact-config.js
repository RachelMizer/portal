// Google Form embed configuration for contact.html
//
// To wire up the real form:
//   1. Open your Google Form -> Send -> the "<>" (embed HTML) icon.
//   2. Copy the URL inside the src="..." attribute of the <iframe> it gives you.
//   3. Paste it below as PORTAL_CONTACT_FORM_URL.
//
// Until this is set, contact.html shows a plain mailto fallback instead of a
// blank/broken iframe.
window.PORTAL_CONTACT_FORM_URL = "";
